# This is a boilerplate parameters config generated for pipeline 'evaluate_impacts'
# using Kedro 0.19.1.
#
# Documentation for this file format can be found in "Parameters"
# Link: https://docs.kedro.org/en/0.19.1/configuration/parameters.html

drop_vehicles:
  dead_energy_col: dwell_start_kwh

derived_cols:
  initial_rename:
    dwell_start_kwh: dwell_start_veh_kwh

events_from_dwells:
  seq_names:
    - dwell_start
    - dwell_end

report_by_hex:
  id_cols:
    location: hex_id
    time: timestamp_utc
  event_col: hex_kw_diff

add_geometries:
  hex_col: hex_id
  crs: EPSG:4326