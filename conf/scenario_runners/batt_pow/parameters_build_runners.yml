# This is a boilerplate parameters config generated for pipeline 'build_runners'
# using Kedro 0.19.5.
#
# Documentation for this file format can be found in "Parameters"
# Link: https://docs.kedro.org/en/0.19.5/configuration/parameters.html

slurm_command: local # Valid values are "sbatch" and "salloc"

slurm_resources:
  partition: serc
  ntasks: 2 # I believe that this is what we need for SequentialRunner, while ParallelRunner needs n_examples + 1
  cpus-per-task: 1 # Using multiple CPUs takes a lot of effort (e.g. Dask cluster)
  mem-per-cpu: 32GB # Increasing memory, since I've had some memory errors
  gpus: 0
  time: 0-00:45 # Reducing time requirement, since with more processors we're going faster.
  array: 0-3 # 2 at a time is enforced by Gurobi license

cmd_line_calls:
  prefix: rye run
  kedro:
    tags: scenario_run
    # params: Use this for command line parameters

scenario_params:
  builder: BatteryPowerScenarioBuilder
  display_name: batt_pow
  battery_capacities_kwh: # These are only used for home-base, Class 8 vehicles
    - 500
    - 1000
  charge_speed_kw_depot:
    - 75
    - 350
  is_mandate_active:
    - True
    - False